[project]
name = "tindie-products"
version = "0.1.0"
description = "Product and inventory manager for the whatnick Tindie energy monitor store"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "pyyaml>=6.0.1",
    "rich>=13.7.0",
    "click>=8.1.7",
    "python-dotenv>=1.0.0",
    "playwright>=1.58.0",
]

[dependency-groups]
dev = []
